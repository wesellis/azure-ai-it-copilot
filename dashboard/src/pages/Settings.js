import React from 'react';
import { Box, Typography } from '@mui/material';

function Settings() {
  return (
    <Box>
      <Typography variant="h4">Settings</Typography>
      <Typography variant="body1" mt={2}>Application settings coming soon.</Typography>
    </Box>
  );
}

export default Settings;